import FlowBuilder from '@/components/flow-builder/FlowCanvas'

// Campaign flow-builder POC — standalone demo route (visual only, no persistence).
export default function FlowBuilderPage() {
  return <FlowBuilder />
}
